---
    - name: Check out custom repositories
      git: repo={{ custom_git_base }}{{ item.name }} dest={{ drupal_sites_dir }}/{{site_name}}/www/{{item.dest}} version={{ item.version }}
      with_items: custom_repos
      when: custom_repos is defined

